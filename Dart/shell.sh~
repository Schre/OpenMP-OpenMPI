#!/bin/bash
val=1; #procs
n=100000000
echo "part1"
for i in $(seq 1 4);do # for each input size, run with changing processes


#for j in $(seq 1 6);do
if [ $n -ge $val ]; then
    ./a.out $n $val 
fi
val=$(($val*2))
done


echo "part2"
n=156250
val=1
for i in $(seq 1 6); do

if [ $n -ge $val ]; then
    ./a.out $n $val
fi
val=$(($val*2))
n=$(($n*2))
done


#n=$(($n*2)) # vary input size...

#rm file*
#done;
